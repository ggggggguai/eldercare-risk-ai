# 徘徊识别技术文档2

版本：2.8
更新时间：2026-08-13
状态：现行唯一技术路线

> 本文只回答四件事：要做成什么模型、怎样尽快跑通、怎样判断是否达到 95%、怎样准备目标摄像头数据。
>
> 本文不保存逐次故障、Git 提交号、文件哈希、测试计数或历史审批过程。当前任务只看 [任务表](../../../tasks/README.md)，已实现能力只看 [心理健康模块 README](../README.md)，实验结果只看 `reports/mental_health/`。旧方案已经移动到 [历史归档](../../../../文档/徘徊样行为识别技术方案（历史归档-2026-08-12）.md)，仅用于追溯。

---

## 0. 一页执行摘要

路线分成两条可并行主线；实时状态仍只由任务表维护：

1. **模型线**：M0-S、M0-R Release-Prep v2、M0-RH score-entry hardening v3、M0-RS 固定 WP public-holdout 计分、`M0-CAM-E` primary camera synthetic 工程闭环和 `M0-CAM-H` 三项小型加固均已完成；M0-RS 达到预注册门槛且结果已经固化，不执行 M1，也不按该 test 回调模型。当前先完成不依赖真人数据的 `M0-CAM-RD` 数据就绪工具；`M0-CAM-D` 工程 smoke 等待 RD+C0+C1，有标签评估再等待 C2+C3。实时任务只看任务表。
2. **数据线 C0-C3**：完成目标摄像头授权、工程 pilot、episode 标注与 development 分组，建立视频、轨迹、episode、人工标签和合法评估 cohort 的连接方式。

当前模型开发不再等待 Step10-B、双环境逐字节重建或更多治理检查。优先交付：

```text
公开轨迹数据
  → 固定预处理
  → TCN + relation-aware Transformer
  → binary/subtype 分类头
  → M0 单 seed 训练、best checkpoint、新进程重载（已完成）
  → M0-S 固定 3 seeds
  → paired RF/TCN、成本与错误稳定性
  → development 候选冻结
  → M0-R candidate manifest、紧凑 inference bundle、WP-only evaluator（v2 已完成）
  → M0-RH 计分入口加固：受信 accessor、固定 CPU runtime、源码身份预检（v3 已完成）
  → M0-RS：固定 WP public holdout 候选级计分（已完成，target_met）
  → M0-CAM-E：primary camera synthetic 工程接入（已完成，synthetic_contract_only）
  → M0-CAM-H：证据边界、二分类平票规则、执行源码身份三项小型加固（已完成）
  → M0-CAM-RD：camera development 数据就绪工具（当前；不读取真人数据）
  → C0-C3：授权、tracking/sidecar、annotation 与分组数据闭环
  → M0-CAM-D：授权 camera development smoke / 有标签评估（分别等待 RD+C0+C1 / C2+C3）
  → C4 sealed camera
```

公开数据阶段的最低性能目标：

- WanderingPatterns 固定 validation 四分类 macro-F1 ≥ 0.95；
- WanderingPatterns + SmartCare 二分类来源等权 macro-F1 ≥ 0.95；
- direct、pacing、lapping、random 每类 recall 以 ≥ 0.90 为建议目标；
- 必须同时报告 confusion matrix 和原始样本数。

这里的“95%”不是只看 accuracy。类别不平衡时，accuracy 可能掩盖少数类别失败，因此以 macro-F1 为主要指标。

达到公开数据 0.95 只表示形成了公开轨迹上的开发候选，不代表目标摄像头、真实老人或临床效果。摄像头能力必须使用独立授权和标注的数据另行评估。

---

## 1. 文档职责与单一事实源

为避免再次出现 5,000 多行方案和多处状态冲突，文档职责固定如下：

| 文件 | 唯一职责 | 禁止写入 |
| --- | --- | --- |
| 本文 | 稳定目标、模型结构、数据口径、执行阶段和验收标准 | 逐次报错、commit/hash 流水、实时进度 |
| `docs/tasks/README.md` | 当前唯一任务、负责人和下一交付物 | 长篇技术设计、历史结果 |
| `docs/modules/mental_health/README.md` | 已实现能力和当前限制 | 详细未来执行协议 |
| `reports/mental_health/...` | 每次实验的配置、命令、指标、曲线和失败案例 | 未来路线 |
| [协作交接文档](../徘徊模块协作交接与职责边界.md) | 角色、输入输出接口和何时需要负责人 | 重复维护实时状态 |
| `文档/徘徊样行为识别技术方案（历史归档-2026-08-12）.md` | 旧步骤、旧治理协议和历史追溯 | 现行指令 |

如果这些文件出现冲突，优先级为：

1. 用户最新明确要求；
2. 当前代码、数据和实际报告；
3. 本文的稳定路线与边界；
4. 任务表的当前安排；
5. 历史归档。

---

## 2. 总目标、指标和能力边界

### 2.1 总目标

交付一套基于轨迹的徘徊识别模型：

- 输入为固定长度的人体轨迹和逐点运动特征；
- 主体结构为 mask-aware TCN + relation-aware Transformer；
- 输出 direct、pacing、lapping、random 四种轨迹形态；
- 同时输出 direct/non-wandering 与 wandering-like 二分类结果；
- 支持训练、best checkpoint、断点恢复、新进程重载和推理；
- 最终能够接收目标摄像头跟踪轨迹并输出 episode 级结果。

本文的模型与摄像头交付边界止于 episode 级轨迹形态或告警候选。C4 完成不等于心理健康预警主链完成：将 episode 汇总为日级 evidence，绑定稳定 person identity，接入个人基线与 `routine_irregularity_score`，再生成 `AlgorithmEvent(module=mental_health)`，属于独立的后续集成任务，需要另行实现和验证。

### 2.2 公开数据开发目标

| 任务 | 数据 | 主指标 | 最低目标 |
| --- | --- | --- | ---: |
| 四分类 | WanderingPatterns validation，240 条 | macro-F1 | ≥ 0.95 |
| 二分类 | WP 240 + SmartCare 38，按来源等权 | source-equal macro-F1 | ≥ 0.95 |
| 类别安全线 | direct/pacing/lapping/random | 每类 recall | 建议 ≥ 0.90 |

二分类 `source-equal macro-F1` 固定为：

```text
(WP binary macro-F1 + SmartCare binary macro-F1) / 2
```

不按两个来源的样本数直接池化，以免 WP 样本量掩盖 SmartCare 表现。

附加报告：

- accuracy 与 balanced accuracy；
- 每类 precision、recall、F1；
- confusion matrix；
- wandering positive recall；
- 样本数、训练时间、推理时间；
- best epoch 和训练曲线；
- 代表性错误样本。

M0 首次训练未达到 0.95 时，仍可判定“模型闭环完成、性能待优化”。不得因为指标暂时较低而把已经跑通的训练链重新写成未完成。

### 2.3 目标摄像头效果目标

在取得独立、授权、隔离的 sealed 摄像头数据后，分两层评价，不能把两层结果混成一个“识别率”：

- **轨迹形态层**：TCN + Transformer 只根据轨迹评价 direct、pacing、lapping、random；目的性 pacing 在这一层仍是真实 pacing，不能改成 direct；
- **告警决策层**：如需把目的性 pacing 作为不告警的困难负例，必须另有运行时可观察的场景/活动上下文或 episode 决策层。只有该层实现后，才评价 wandering-alert episode precision、recall、F1。precision 与 recall 点估计均达到 0.95 是产品方向目标，不是 M0、纯轨迹模型或首次 C4 的验收承诺；
- 两层分别报告 subtype macro-F1、逐类 recall 和混淆；
- false alarms / eligible negative person-hour；
- 首次识别延迟；
- 可评分轨迹覆盖率；
- `uncertain/unavailable` 比例；
- track fragment 和 ID switch 造成的失败。

摄像头指标必须给出原始分子、分母和置信区间。数据量不足时只能称为 pilot，不得声称现实识别率达到 95%。

未达到产品目标时，只能在 development 数据上改进可观察的上下文 gate、概率校准、阈值和 episode 合并。低置信度、OOD 或上下文不足时输出 `uncertain`，tracking/QC 失败时输出 `unavailable`；证据不足的候选进入人工复核，而不是自动作高置信告警。不得根据 sealed 结果回调同一版本；继续开发时必须进入新的 development 周期，并使用新的独立 sealed 数据复评。没有可靠上下文输入时，只能报告诊断性的 shape-trigger 误报率，不能宣称具备目的感知告警能力。

### 2.4 不属于本阶段的结论

本模型识别的是可观察轨迹形态，不直接判断人的意图、认知障碍、抑郁、焦虑或临床风险。

以下表述当前禁止：

- “真实老人识别准确率 95%”；
- “摄像头场景已经验证”；
- “徘徊等同于认知障碍”；
- “模型输出可替代人工照护或医学诊断”。

---

## 3. 当前技术起点

截至 2026-08-12，项目已经具有：

- WanderingPatterns 与 SmartCare 的转换、固定 split 和统一预处理；
- 80 点、14 通道轨迹输入；
- 26 维手工特征 + Random Forest 对照；
- 纯 TCN 二分类与四分类对照；
- bbox-bottom 摄像头适配、轨迹质量控制和离线推理链；
- 合成跟踪污染和兼容性诊断；
- TopoWander-MPT 的 TCN、semantic patch、relation Transformer 和分类头前向实现；
- TopoWander-MPT M0 的单模型双头监督训练、joint evaluator、best/last checkpoint、fresh reload 和单 seed development-validation 报告；
- TopoWander-MPT M0-S 的固定三 seed 稳定性、异常恢复加固、fresh reload、paired RF/TCN 解释与 primary seed 冻结；
- M0-R Release-Prep v2 的 manifest-bound 紧凑 inference bundle、CPU/`eval`/`inference_mode` loader、WP-only evaluator，以及 240 条 WP validation 的逐样本零差异 parity；
- 位于外部历史工作树、尚未合入当前 checkout 的 Step10-A 五 seed 自监督预训练候选。

重要基线：

| 模型 | 四分类 validation macro-F1 | 二分类来源等权 macro-F1 |
| --- | ---: | ---: |
| 26 维手工特征 + RF | 0.9726 ± 0.0033 | 0.9507 ± 0.0069 |
| 纯 TCN | 0.9411 ± 0.0105 | 0.9967 ± 0.0011 |

Step10-A 的 total proxy 平均改善约 0.0437%，尚未证明分类收益。当前 checkout 没有稳定的 bundle 路径，也没有 trunk-only loader，因此 M0 默认使用同结构 scratch 初始化；只有候选位置被明确登记、兼容性验证通过并补齐安全的 trunk-only 加载能力后，A-init 才作为可选对照，不能阻塞监督模型闭环。

2026-08-12 的 M0-S 已用固定 seeds `20260731/20260801/20260802` 完成稳定性确认。三次 WP 四分类 macro-F1 均为 `0.987497`，来源等权二分类 macro-F1 为 `0.997238 / 0.983848 / 0.997238`，全部已报告类别 recall 最低为 `0.941176`；primary seed 按预注册规则固定为 `20260731`，没有进入 M1。完整结果见 [M0-S 报告](../../../../reports/mental_health/wandering_performance/m0s_three_seed_stability_v1/README.md)。WanderingPatterns 没有可靠 participant/session 分组，现有审计记录了 4,054 对 `<0.05` 的跨分区形状近邻，因此高分仍可能对独立人员、目标摄像头和真实老人明显乐观。

M0-RH 已完成正式入口加固、v3 release identity、candidate manifest 和 development validation parity；M0-RS 随后完成固定 WP public-holdout 候选级计分并达到预注册门槛。模型线后续拆成两门：

1. `M0-CAM-E`：已用 synthetic fixture 跑通同一 primary bundle 的逐窗预测、QC/fallback 与最小 episode-candidate 聚合；
2. `M0-CAM-H`：已在不读取真人数据、不改候选的前提下，完成进入 development 前的三项 fail-closed 加固；
3. `M0-CAM-RD`：只用 synthetic fixture 建立 collection/session、annotation、分组 readiness 和 authorized-development 控制流，使合法数据到位后可以直接评估；
4. `M0-CAM-D`：RD+C0+C1 后可先做无标签工程 smoke；C2+C3 形成合法真值和分组后，才验证逐窗/episode 指标、purposeful hard negatives、eligible negative person-hours 和目标机位域差异；
5. 模型、阈值与 episode 规则冻结后执行独立 C4 sealed camera 评估。M0-RS 的 WP test 结果不得用于回调模型、阈值、split、标签或协议。

---

## 4. 数据、标签和 split

### 4.1 公开数据

当前 ready 数据：

| 分区 | WanderingPatterns | SmartCare | 合计 |
| --- | ---: | ---: | ---: |
| train | 1120 | 137 | 1257 |
| validation | 240 | 38 | 278 |
| WP public holdout（历史名称 frozen WP test，同一 240 条） | 240 | 0 | 240 |
| SmartCare official sealed | 0 | 20 | 20 |

另有 15 条 SmartCare train 因有效点过少标记为 unavailable，不进入 tensor、loss 或指标。

### 4.2 监督任务

四分类只使用 WanderingPatterns：

```text
direct
pacing
lapping
random
```

二分类使用 WanderingPatterns 与 SmartCare：

```text
direct_or_non_wandering
wandering_like
```

训练规则：

- 所有具备合法二分类标签的 train 样本进入 binary loss；
- 只有 pacing、lapping、random 样本进入 subtype loss；
- direct 不进入 subtype loss；
- SmartCare 不被强行映射为 pacing/lapping/random；
- validation 不反向传播；
- augmentation 只能由 train 派生。

### 4.3 防泄漏的最低规则

只保留真正影响模型结论的规则：

- split 必须先于切窗和 augmentation；
- 同一原始轨迹及其增强视图不能跨 split；
- validation 用于 early stopping 和开发评估；
- WP public holdout（代码/历史名称 `frozen_wp_test`）只在最终候选冻结后用于候选级计分；
- SmartCare official/raw 不用于训练、调参、选 epoch 或错误分析；
- 目标摄像头按 participant + session/date + setup 分组后再切窗；
- 不能丢弃 tracking/QC 失败样本来制造更高摄像头准确率。

---

## 5. 固定模型：TCN + Transformer

### 5.1 输入

```text
model_features             [B, 80, 14]
shape_normalized_points    [B, 80, 2]
point_mask                 [B, 80]
```

14 通道继续复用现有预处理定义。mask 必须贯穿 TCN、patch、attention 和 pooling，padding 或无效点不能参与特征聚合。

### 5.2 主体结构

```text
80 点轨迹与逐点运动特征
  ↓
mask-aware TCN
  ↓
局部时序表示
  ↓
TCN patches + 几何 semantic patches
  ↓
relation bias + Transformer
  ↓
CLS 全局表示
  ├─ binary head: direct / wandering
  └─ subtype head: pacing / lapping / random
```

组件职责：

- **TCN**：捕获短时间位移、转向、速度变化和局部重复模式；
- **semantic patch**：提供方向反转、回访、闭环等可解释几何摘要；
- **relation-aware Transformer**：建模相隔较远的轨迹片段之间的往返、闭环和全局形状关系；
- **binary head**：判断是否 wandering-like；
- **subtype head**：在 wandering-like 条件下区分 pacing、lapping、random。

TCN 作为序列建模起点与已有研究中卷积序列模型的有效性一致；Transformer 用于跨片段的长程关系。相关方法依据见：

- TCN: https://arxiv.org/abs/1803.01271
- Transformer: https://papers.nips.cc/paper_files/paper/2017/hash/3f5ee243547dee91fbd053c1c4a845aa-Abstract.html
- 轨迹 masked pretraining 参考: https://openaccess.thecvf.com/content/ICCV2023/html/Chen_Traj-MAE_Masked_Autoencoders_for_Trajectory_Prediction_ICCV_2023_paper.html
- tracking error 鲁棒性参考: https://openaccess.thecvf.com/content/CVPR2022/html/Weng_Whose_Track_Is_It_Anyway_Improving_Robustness_to_Tracking_Errors_CVPR_2022_paper.html

这些论文只提供方法启发，不等于已经证明本项目模型有效。

### 5.3 分层概率

四分类概率保持：

```text
P(direct) = 1 - P(wandering)
P(pacing) = P(wandering) × P(pacing | wandering)
P(lapping) = P(wandering) × P(lapping | wandering)
P(random) = P(wandering) × P(random | wandering)
```

M0 首轮不改成直接四分类头。只有在结果显示 binary gate 的错误持续传递到 subtype 时，才把“直接四分类主头 + binary 辅助头”作为后续候选。

### 5.4 初始化策略

M0 默认使用同结构 scratch 初始化，先得到第一份可运行、可解释的监督基线。执行者不得先遍历临时 worktree、猜测 bundle 路径或为恢复旧 Step10 runtime 延迟 M0。

Step10-A primary seed 20260731 只作为后续可选 A-init 对照。启用前必须同时满足：

- 外部候选的稳定路径、manifest 和目标 seed 文件已在本次实验配置或报告中明确登记；
- 候选与当前 `TopoWanderMPT` 的 trunk 参数名、shape 和 dtype 已验证兼容；
- 新建并测试了只加载 trunk 的明确映射逻辑；
- binary/subtype heads 保持重新初始化；
- 不覆盖 scratch 结果，也不把候选复制到未登记的位置。

现有 `safe_load_topowander_model()` 是严格的完整 `state_dict` loader，不是 trunk-only loader；不得通过忽略 missing/unexpected keys、删掉分类头数组或放宽 strict 校验来伪装成 A-init。真正启用 A-init 时：

- 只加载 trunk 相关参数；
- binary/subtype heads 重新初始化；
- projection/reconstruction/denoise 分支不参与监督分类 loss；
- 整个 trunk 和两个分类头端到端训练。

旧 `configs/modules/wandering_topowander_mpt_v1.yaml` 是 Step9 的 forward-only 架构契约，不是 M0 训练配置，其中 CPU 单线程、禁止 optimizer/training 等旧验证字段不约束监督训练。M0 只复用 `TopoWanderMPT` 结构；设备、线程、optimizer、epoch、checkpoint 和 resume 由新的 performance config 管理。

M0 使用两份分离配置：performance config 通过 `model.forward_config_path` 引用 `configs/modules/wandering_topowander_mpt_v1.yaml`；模型必须用 `load_topowander_config()` 与 `create_topowander_model()` 从该 forward config 构造，optimizer、epoch、device、checkpoint 等只从 performance config 读取。不得把 performance config 整体或合并后的字段树传入 `TopoWanderMPT`。

### 5.5 损失

建议首版：

```text
total_loss =
    binary_loss
    + subtype_loss_weight × masked_subtype_loss
```

要求：

- binary 使用 BCEWithLogitsLoss 或等价稳定实现；
- subtype 使用 CrossEntropyLoss；
- subtype loss 只在合法 wandering subtype 样本上计算；
- class/source weight 必须在 train 内计算并写入配置；
- 不使用 validation 调权重；
- loss、梯度和模型输出必须 finite。

---

## 6. M0：先跑通完整监督模型

当任务表激活 M0 时，按本节完成完整监督闭环。M0 完成前不替换 TCN、Transformer、semantic patch、relation bias 或分类头；当前是否正在执行 M0，只由任务表维护。

**M0 不是 Step6 的双模型结构。** 不得直接照搬 `train_tcn_baseline.py`：Step6 为四分类和二分类分别训练独立模型、独立 optimizer 和独立 early stopping；M0 必须使用一个共享 TCN + Transformer trunk、一个 binary head、一个 subtype head、一个 optimizer 和一条联合 checkpoint 选择规则。每个 M0 batch 必须同时装配并传入：

```text
model_features
shape_normalized_points
point_mask
```

可以复用 Step6 的 preprocessing bundle 接入、标签口径和基础指标计算逻辑，但不能复用其双任务训练控制流。

### 6.1 建议实现文件

```text
src/elderly_monitoring/modules/mental_health/wandering/performance.py
scripts/wandering/train_wandering_performance.py
configs/modules/wandering_performance_v1.yaml
tests/test_wandering_performance.py
reports/mental_health/wandering_performance/
```

路径可以在徘徊模块内合理调整，但训练、评估、重载和报告职责必须清楚，不能把全部逻辑塞进一次性内联命令。

M0 实施前仓库没有 joint evaluator；当前已经由 `performance.py` 实现。它从同一个模型和同一份逐样本预测推导 WP 四分类指标，分别计算 WP、SmartCare 二分类及 source-equal macro-F1，并输出联合 `selection_score`、逐类 precision/recall/F1、confusion matrix 和逐样本预测。M0-S 必须复用这一 evaluator，只为多 seed 汇总做必要扩展，不能另建口径不同的 evaluator；现有 RF/TCN 单任务 evaluator 仍只作 paired baseline 输入。

### 6.2 M0-0：训练前烟测

先用极小 train 子集运行 overfit smoke：

- 至少包含 direct、pacing、lapping、random；
- loss 应明显下降；
- binary/subtype head 都有梯度；
- trunk 参数发生更新；
- mask 后的 padding 不影响结果；
- checkpoint 保存后可以重载。

smoke 不能拟合时，优先修复标签映射、loss mask、head、优化器或数据装配，不启动长训练。

### 6.3 M0-1：单 seed 正式训练

首轮建议：

- seed：20260731；
- epoch 上限：30–50；
- 允许 validation early stopping；
- 允许合理 CPU 多线程或 GPU；
- 使用适合显存/内存的 batch size；
- 每个 epoch 打印 train loss、validation loss、四分类 macro-F1、二分类 source-equal macro-F1、学习率和耗时；
- 第 1–2 个 epoch 后输出平均 epoch 时间和 ETA；
- 保存 latest checkpoint 和 best checkpoint；
- 支持从 latest 恢复；
- 若到 50 epoch 指标仍稳定提高，可以继续训练并记录原因。

best checkpoint 使用预先固定的联合选择规则：

```text
selection_score = min(
    WP 四分类 validation macro-F1,
    WP+SmartCare 二分类 source-equal macro-F1,
)
```

先选择 `selection_score` 最高的 epoch；并列时依次选择四分类 macro-F1 更高、二分类 macro-F1 更高、validation loss 更低、epoch 更早者。这样不会为了抬高一项指标而选择另一项明显不达标的 checkpoint。最终报告仍分别给出两个指标，不能只报告 `selection_score`。

### 6.4 M0-2：独立重载与报告

训练完成后必须在新进程：

1. 加载配置和 best checkpoint；
2. 重建同一 TCN + Transformer；
3. 对 validation 生成逐样本预测；
4. 计算四分类和二分类指标；
5. 运行最小单批推理；
6. 输出结构化 metrics JSON、predictions 文件、confusion matrix 和 README。

最低报告内容：

- 数据版本和样本数；
- 初始化方式；
- seed、epoch 上限、best epoch；
- 参数量；
- 总训练时间、平均 epoch 时间；
- WP 四分类 macro-F1；
- 四类 precision/recall/F1；
- confusion matrix；
- WP 与 SmartCare 二分类分来源指标；
- source-equal macro-F1；
- wandering positive recall；
- best checkpoint 路径；
- fresh reload 结果；
- 代表性错误样本；
- 当前最大短板。

### 6.5 M0 完成定义

满足以下条件即为 M0 闭环完成：

- runner/config/test 可运行；
- 小样本 overfit smoke 通过；
- TCN + Transformer 完成端到端监督训练；
- best/last checkpoint 已形成；
- best checkpoint 能在新进程重载；
- validation 四分类与二分类报告完整；
- M0 训练、validation、fresh reload 和选点路径没有使用 WP test；WP raw、SmartCare official/raw 与 sealed camera 没有进入本次模型流程；
- 没有覆盖旧 Step10-A 或历史报告。

是否达到 0.95 是性能状态，不是“模型有没有做出来”的判断：

| 结果 | 状态 |
| --- | --- |
| 闭环完成，四分类与二分类 macro-F1 均 ≥ 0.95 | M0 达到公开数据最低目标 |
| 闭环完成，但任一 macro-F1 < 0.95 | M0 已完成，进入性能优化 |
| 训练/重载/报告未闭环 | M0 未完成 |

---

## 7. M0 后的候选稳定与按需性能提升

### 7.1 先按 M0 结果分流

M0 达到两项 `0.95` 后，不再为了“看起来更复杂”自动进入 M1。当前路线固定为：

```text
M0 两项 macro-F1 均达到 0.95
  → 跳过 M1
  → M0-S 三 seed 稳定性确认
  → paired RF/TCN 解释 + 成本复核
  → 冻结 primary-seed development 候选
  → M0-R Release-Prep + M0-RH score-entry hardening
  → M0-RS 固定 WP public-holdout 计分（已完成，target_met）
  → M0-CAM-E：primary camera synthetic 工程闭环（已完成）
  → M0-CAM-H：进入 development 前的三项小型加固（已完成）
  → M0-CAM-RD：不依赖真人数据的 development 数据工具（当前）
  → M0-CAM-D：RD 与明确授权数据均就绪后开展 camera development 评估

M0 任一主指标未达到 0.95
  → 根据最大短板做一个有界 M1
  → 重新完成同口径 validation
```

一次只处理一个主要短板；指标有实际改善且关键类别没有明显退化才保留。小于约 0.5 个百分点的变化在当前 240 条 WP validation 上可能只对应 1–2 个样本，先视为暂定改善，不为此展开大规模消融或网格搜索。

### 7.2 M0-S：三 seed 稳定性确认与 development 候选冻结

M0-S 不是新模型实验，也不更换组件。它只回答当前高分是否依赖一次随机初始化，并把已经达到目标的模型整理为可追溯的 development 候选。

固定要求：

1. seeds 总计只用 `20260731、20260801、20260802`，不临时追加 seed；
2. 使用同一最终源码、scratch 初始化、模型结构、development bundle、split、loss、optimizer、batch size、最大 epoch、early stopping 和 checkpoint 选点规则；
3. 多 seed 支持必须由 config/CLI 明确传入，实际 seed 写入 resolved config、checkpoint metadata、predictions 和 fresh report，不能靠未记录的临时 YAML 改写；
4. 在新的 M0-S 命名空间重新运行全部 3 seeds，原 `m0_seed20260731_v1` 报告和 checkpoint 保持不变；
5. 每个 seed 都执行 best/last、fresh-process reload 和相同 validation evaluator；
6. 直接复用 RF/TCN 已保存的同 split、同 seed 逐样本预测做 paired 比较，不重训 RF/TCN，不要求显著性检验；至少报告 `both_correct / M0_only_correct / baseline_only_correct / both_wrong`、macro-F1 差值和逐类 recall 差值；
7. 汇总每个 seed 的 WP 四分类、WP/SmartCare 二分类、来源等权二分类、逐类 recall、best/last epoch、训练时间、推理时间、错误交集和并集；
8. 三个 seed 的两项主 macro-F1 均应不低于 `0.95`，全部已报告类别 recall 均应不低于 `0.90`；同时报告 mean/std/min，而不是只展示最好一次；
9. 通过后固定 `20260731` 为 primary seed，不挑最好 seed，也不把 3 seed 临时组成 ensemble；
10. M0-S 模型路径不得通过 frozen accessor 取得 WP test，也不得让其进入 tensor、推理、计分、训练或选点；WP raw、SmartCare official/raw 与 sealed camera 不得读取。M0-S 通过后先冻结 development 候选，WP public holdout 留给单独批准的一次候选级计分。

进入三次最终训练前只做三项小型工程加固，不扩展成治理项目：

- checkpoint/history/latest 的提交或恢复逻辑要能处理一个真实的中途异常窗口，并用最小故障注入测试证明可以继续；本次 CPU M0 已证明 model、AdamW、CPU RNG 与 epoch 可以完整加载，但“已完成 run 再加载”不等于任意中断点都已验证；
- 若 config 允许 CUDA resume，则同时保存/恢复 CUDA RNG；否则要显式说明 CUDA resume 尚不支持，不能继续宣称“完整 RNG 恢复”；
- development accessor 会为完整性解析共享 preprocessing container，其中包含 WP test 行；M0-S 元数据必须准确写成“M0-S 训练、validation、fresh reload 与选点路径没有使用 test”，不得再写成整个仓库从未读取或暴露 test。SmartCare official/raw 与 sealed camera 没有进入本次模型流程。

M0-S 通过才可称“稳定的公开轨迹 development 候选”。若任一 seed 未达到上述口径，状态写为 `seed instability`，保留全部结果并分析波动来源；不得挑掉失败 seed、改门槛或直接用 WP public holdout 给候选计分。

### 7.3 M0-R：固定候选发布准备与 WP public-holdout 计分

M0-R 不再训练模型，也不替换组件。它把 M0-S primary 整理为可独立加载的紧凑推理候选，并在独立授权后对固定 WP public holdout 做一次候选级计分。

科学边界必须准确：WP test 已被仓库契约测试读取，并已用于 Step5 RF 与 Step6 TCN 的历史评分；M0-R 不是项目级首次盲测，更不是目标摄像头或真实老人验证。它只回答固定 TopoWander-MPT primary 在同一 public-shape benchmark 上的表现。

M0-R 分三段。2026-08-12 已完成第一段 Release-Prep v2 和第二段 M0-RH v3：固定候选始终为 `seed=20260731 / best epoch=5`，精确身份与 SHA 以 M0-R/M0-RH 报告为准；v3 已把受信 test accessor、固定 CPU runtime、实际执行源码和 training/release 双身份同时绑定，并在 240 条 WP validation 上得到 ID/标签、概率和 WP-only 指标零差异 parity。2026-08-13 已完成第三段 M0-RS 固定候选计分，结果达到预注册门槛并固化；后续不因该结果重训或回调模型。

1. **Release-Prep（已完成；新候选模型路径未使用 WP test）**
   - 固定 M0-S primary `seed=20260731 / best epoch=5`，不得改用历史 M0 epoch 13、其他 seed、ensemble 或重新训练结果；
   - 生成 candidate manifest，分别记录不可变的 `training_candidate_identity`（M0-S best model、checkpoint metadata、原 source bundle、forward/performance config 与数据身份）和 `release_implementation_identity`（新 loader/evaluator/CLI 的 commit 或文件摘要）；新增发布代码不得覆盖或重写原训练身份。manifest 还要固定 binary `sigmoid >= 0.5`、四分类层级概率在 `[direct,pacing,lapping,random]` 上 argmax，以及 subtype 顺序 `[pacing,lapping,random]`；
   - 导出只含完整 model state、配置和 manifest 的紧凑 inference bundle，不携带 AdamW、RNG 或三 seed 全部 epoch 历史。现有 strict loader 要求完整 state，因此不能删去 projection arrays 后伪称同一候选；
   - 在现有 `safe_load_topowander_model()` 外增加 candidate wrapper：caller 必须提供外部可信的 candidate-manifest SHA，wrapper 先校验 manifest 原始字节、相对路径/边界及 model/config SHA，再调用 safe loader；加载后固定 CPU、`eval()`，推理固定使用 `torch.inference_mode()`，不能让同一份未验证 manifest 自认证；
   - 新增 phase-aware 的 WP-only record validator/dataset/evaluator，显式接受 `expected_split=validation|test`。不得放宽原 development validator，也不得把 test 行改写成 validation。test cohort 固定为 source=WP、240 条、四类各60，binary `0/1=60/180`；
   - 优先新建独立 release 模块/CLI，保持模型权重、forward config 和训练选点不变；若抽取共享指标函数，必须以 primary validation parity 证明行为不变，但不能据此重写 M0-S source identity；
   - 用 synthetic fixture 和 primary validation 完成 fresh-process load、逐样本预测、metrics/confusion 输出与 validation parity。parity 固定使用 M0-S 相同 CPU runtime 和 `batch=64`；240个WP validation sample ID与label必须精确一致，逐样本probability及WP-only指标 `max_abs <= 1e-7`。基准四分类 macro-F1 为 `0.9874973951700553`、WP binary macro-F1 为 `0.9944750109348742`，不能拿来源等权 `0.997238` 代替；
   - Release-Prep 的模型/evaluator路径不得使用 WP test。既有 accessor 契约回归可照常解析固定 WP test 记录以验证 access/count/schema，但不得运行 M0-R 候选推理、计分或产生设计反馈；
   - 源码身份需由选择性本地 commit或等价不可变源码包持久化，不能只记录hash却丢失原字节；只纳入本任务文件，不混入其他模块或用户脏改动，不push。紧凑二进制bundle保存到报告声明的持久artifact位置，记录路径、size、SHA；若尚无远端artifact store，必须明确为“当前机器local-only”，不能写成可远端获取；
   - 输出目录拒绝覆盖。路径、timeout、WSL/Windows 或普通实现错误由执行 AI 自主修复、复测和继续，不恢复旧 Step10 的逐异常请示。
2. **M0-RH 计分入口加固（已完成；新增候选/score 路径未使用 WP test 做推理或计分）**
   - 新增唯一正式入口 `score-frozen-wp`（名称可等价，但语义必须唯一）。建议对应 API 为 `run_authorized_frozen_wp_score(...)`；CLI 只接收 `--project-root`、`--manifest`、`--expected-manifest-sha256` 和 `--output-dir`，不得接收 records、split、batch、threshold 或线程参数。它在取得计分授权后，内部调用现有 `load_preprocessing_bundle(..., mode=BUNDLE_MODE_FROZEN_WP_TEST)`，从受信 accessor 取得 `records_for_split("test")`；正式 test 模式不得接受调用者提供的任意 `--records` JSONL。现有 `evaluate-records` CLI 必须限制为 validation-only；内部 evaluator 可以保留 synthetic test fixture 能力，但不能形成生产旁路；
   - 正式入口必须在未来已授权计分时，把 accessor 已验证的 split、preprocessing config、输入资产 identity，与当次 canonical records SHA-256、ordered sample-ID SHA-256、240 条计数和类别分布写入 execution evidence。M0-RH 只绑定上游 descriptor、accessor mode 和 cohort 契约，不得为预填这两个 cohort SHA 而让新增 controller 提前调用真实 accessor；既有 accessor contract 回归不用于生成这两个 SHA。正式入口也不能靠“240 条且类别平衡”来冒充固定 holdout；
   - candidate manifest 固定 CPU runtime 为 intra-op `8`、inter-op `1`、`batch=64`、`num_workers=0`、`pin_memory=false`。validation parity 和未来 score 必须在 fresh process 中调用同一 runtime 配置并回读确认，不能让正式 score 继承环境默认线程；
   - 在打开正式 test accessor 之前，score 入口必须先拒绝已经存在的 final output，再验证当前实际执行的 release module/CLI 与 manifest 中 `release_implementation_identity` 一致，并验证会影响模型结构、safe loader、validation parity 和 preprocessing accessor 的 `model.py`、`performance.py`、`preprocessing_bundle.py` 等源码仍与冻结身份一致。preflight 还必须验证 v3 frozen source archive 的外层 SHA、exact entry 集合和逐文件 bytes，并把 active execution paths/bytes 与该 archive 交叉一致；如果选择直接从 archive 执行，也必须先完成同等验证。只在 manifest 中保存摘要而不在 score 时复核，不算闭合；v3 manifest 还必须绑定 `configs/modules/wandering_rf_v1.yaml` 及其受信 preprocessing/split 输入描述符；
   - 新增 candidate/controller 路径只使用 synthetic fixture、fake accessor 和 development WP validation 开发、测试。不得为加固生成 test records SHA、运行候选 test inference 或查看 test 分数；完整 wandering 回归中的既有 accessor contract 测试可按原范围解析记录，但不得形成候选输入或设计反馈；
   - 实现稳定后生成新的 release source archive、release identity、candidate manifest v3 和 `validation_parity_v3/`。v1/v2 bundle、manifest 和诊断必须原样保留；v3 仍绑定同一 M0-S model/config bytes，不能重训、改权重、改阈值、改 split 或执行 M1；
   - v3 完成门：候选 model/config SHA 与 v2 相同；240 条 WP validation 的 ID/label exact，逐样本概率和 WP-only 指标相对冻结 reference 的 `max_abs <= 1e-7`；M0-R 窄测与全部 wandering 回归通过；报告、模块 README 和任务表同步；状态写为 `wandering_m0r_score_entry_hardened_waiting_public_holdout_authorization`；
   - 普通实现、路径、Windows/WSL、timeout、测试和报告问题由执行 AI 自主修复并继续。只有必须改变固定候选、科学口径、受保护数据边界、覆盖旧证据、修改其他模块、commit/push，或实际打开真实 public holdout 时才暂停。
3. **M0-RS 固定 public-holdout 候选级计分（已完成，`target_met`）**
   - 只使用上述 primary candidate，对 frozen accessor 暴露的 240 条 WP test 形成一份最终计分结果；不读取 WP raw、SmartCare official/raw 或 sealed camera；
   - 只报告 WP 四分类 macro-F1、四类 recall、confusion matrix、逐样本预测/错误，以及 WP 二分类 macro-F1 和两类 recall；本任务只访问 WP public holdout，SmartCare official 继续 sealed 且未授权，因此没有合法的 SmartCare test cohort，不计算或声称 source-equal test 指标；
   - 目标口径预先固定为：WP 四分类 macro-F1 `>=0.95`、WP 二分类 macro-F1 `>=0.95`，且 WP 四个形状类别与 binary 两类的全部已报告 recall 均 `>=0.90`；
   - 正式 score 先在同一文件系统的 staging 目录完整写出 predictions/metrics/confusion/errors 并校验，再原子提交到事先不存在的 final 目录；提交前不打印或暴露指标。final 目录已经存在时只读核验/恢复交接，不再次推理；
   - 分数落盘后才可读取 Step5/Step6 已保存的同 split、同 primary seed `20260731` test predictions 做 post-hoc paired 解释，不在历史五 seed 中挑最优 baseline，也不重训 baseline；
   - 未产生、未观察分数的路径/timeout/写盘等工程失败，可在候选身份、evaluator语义和 threshold 不变时自主恢复；一旦分数已被观察，该结果即为最终结果。无论通过或未达标都固化并结束本次候选计分，不根据这 240 条 test 的错误改 seed、权重、threshold、标签或组件后重跑。

M0-RS、M0-CAM-E 与 M0-CAM-H 均已结束。当前可连续完成 M0-CAM-RD；它不读取真人数据，只把 collection/session、annotation、分组 readiness 和 future authorized-development evaluator 的软件入口准备好。之后取得 C0+C1 可先执行 M0-CAM-D 工程 smoke；C2+C3 就绪后再做有标签指标、置信度、episode 合并和必要域适配。真正独立的泛化结论留给 C4 sealed camera。

### 7.4 M0-CAM：先完成工程接入，再进入授权数据评估

M0-CAM 按工程接入、数据就绪和授权 development 评估推进；证据范围依次区分 synthetic 模型契约、synthetic 数据 schema 契约、授权工程 smoke 和授权有标签 development evaluation。不能因暂时没有真人视频而阻塞可独立完成的软件准备，也不能把 synthetic 结果冒充目标摄像头效果。

1. **M0-CAM-E：primary camera engineering closure**
   - 完成状态固定为 `wandering_m0cam_primary_camera_engineering_ready`；
   - 保留 Step7 RF/TCN `comparison_only` 配置、CLI、schema 和报告，不覆盖或改写其语义；优先新增独立 primary camera wrapper、CLI、配置和 schema；
   - 复用 `load_camera_inputs()`、`run_camera_qc()`、`prepare_camera_window()`、`load_candidate_from_manifest()` 与公共 `hierarchical_four_class_probabilities()`；只对 manifest 校验后返回的 `runtime.model` 前向，不得绕过 manifest 另载权重。ready 窗输入固定为 `model_features [80,14]`、`shape_normalized_points [80,2]` 和 `point_mask [80]`；
   - `CandidateRuntime.predict_logits()` 是 WP record/split/cohort 专用入口，camera 不得伪装成 WP record 调用，也不得复制 WP 固定 cohort 的 prefix padding。Camera wrapper 应在模型保持 CPU、`eval()` 和 `torch.inference_mode()` 的前提下直接前向；camera batching 是独立工程 runtime，必须固定顺序并记录策略，但不声称复放 M0-RS 的逐字节概率；
   - 输出 binary、subtype、hierarchical four-class 概率、预测类别、窗口时间、scope/tracklet、QC flags、candidate/manifest identity，并固定 `probability_calibrated=false`；
   - `unavailable` 窗必须零 forward；可信模型 forward/output 异常可成为单窗 `inference_error`，candidate/config/schema/输入契约错误必须终止整次构建；
   - episode 层只实现同一完整 scope 与同一 parent tracklet 内的 shape-candidate 合并接口和 deterministic synthetic 测试，策略标记为 `development_unfrozen`。无授权标注数据时不冻结 uncertain、merge gap 或告警阈值，也不报告 episode F1；
   - 正式冻结 candidate 的 fresh synthetic run 必须跑通 adapter→QC→prepared arrays→primary→window predictions→episode candidates→fresh output bundle，并覆盖正常 ready、unavailable、scope 隔离、概率公式、拒绝覆盖和最小 CPU 延迟；`inference_error` 只用 fake model/fault-injection 自动测试覆盖，不得篡改正式 candidate、manifest 或配置制造失败。即使没有真人视频，以上通过后也应完成本阶段，而不是标为 blocked。

   **完成记录：** M0-CAM-E 已于 2026-08-13 完成，状态为 `wandering_m0cam_primary_camera_engineering_ready`，证据等级仅为 `synthetic_contract_only`。v1/v2/v3 synthetic bundle 均保留为历史工程证据。

2. **M0-CAM-H：pre-development hardening**
   - 这是便于当前任务交接的轻量代号，不新增科学证据等级或发布状态机；完成后三项加固记入 M0-CAM-E 报告，状态仍为 `wandering_m0cam_primary_camera_engineering_ready / synthetic_contract_only`，随后任务表转入 M0-CAM-RD；不重训模型、不重建 M0-R 候选、不改写 M0-CAM-E v1/v2/v3 输出；
   - **合成证据边界 fail closed**：现有 M0-CAM-E 配置和 CLI 只允许 sidecar 的 `authorization_status=synthetic_fixture`。该入口可先只读 media sidecar 做 authorization-only 预检；另外两个真人 development 授权状态必须在 candidate loader、tracking JSONL/QC、模型 forward、staging 和 final 输出前明确拒绝，不能继续固定写成 `synthetic_contract_only / real_human_media=false`。M0-CAM-D 应使用独立入口或配置，并按实际授权和数据访问生成准确证据等级；
   - **二分类平票规则统一**：primary config 的数值阈值必须与 candidate manifest 的 `sigmoid >= 0.5` 决策契约一致；window prediction 与 episode validator 都使用 `p(wandering_like) >= 0.5` 判为 `wandering_like`。二分类不得用默认 `argmax` 处理恰为 `[0.5, 0.5]` 的平票；subtype 和 four-class 仍使用固定类别顺序上的 argmax；
   - **执行源码身份预检**：media sidecar 只允许先做 authorization-only 预检；在调用 candidate loader、读取 tracking JSONL/执行 QC 或执行模型 forward 前，核对实际导入的 `model.py`、`release.py` 路径位于当前 project root，并与 v3 candidate manifest 的 release identity 中对应 size/SHA 一致；`model.py` 还必须与 training source identity 交叉一致。漂移时不得产生 staging 或 final 输出。加固逻辑放在 primary camera wrapper，不修改 `model.py`、`release.py` 或 v3 manifest 来迎合检查，也不恢复 M0-RS 的整套发布审计；
   - 完成门保持轻量：为三条行为分别增加回归测试；使用原固定 candidate 在新的、拒绝覆盖的 `synthetic_e2e_v4` 目录重跑一次 fresh E2E；确认 ready/unavailable、零调用、episode 隔离和概率公式仍通过；运行 primary/episode 窄测、相关 Step7/release 回归和 `git diff --check`。完整 wandering 回归可在时间允许时运行，但不是本次局部加固硬门。普通路径、timeout、测试或局部实现问题由执行 AI 自主修复并继续；无需重新训练、访问真人视频、运行 M1 或重复 public-holdout 计分。

   **完成记录：** M0-CAM-H 已于 2026-08-13 完成三项加固和 fresh v4 验收；状态仍为 `wandering_m0cam_primary_camera_engineering_ready`，`hardening_complete=true`，证据等级仍为 `synthetic_contract_only`。当前先完成 M0-CAM-RD；随后 C0+C1 可启动 D 的工程 smoke，C2+C3 就绪后才做有标签评估。

3. **M0-CAM-RD：camera development data readiness**
   - 这是当前可连续完成的软件任务，不需要也不得读取真人媒体。完成状态为 `wandering_m0cam_development_data_tooling_ready`，证据范围为 `synthetic_schema_contract_only`；完成不等于 C0 已授权、数据已采集或 M0-CAM-D 已开始；
   - 建立匿名 collection/session manifest、显式 C0 receipt 前置校验、既有 `TrackObservation JSONL + wandering-media-v1 sidecar` 的核验包装、episode annotation schema/validator、participant/session/setup 分组和 development readiness report。C0 receipt 是负责人/授权流程提供的外部事实，执行 AI 只能验证，不能生成、签署或把 fixture 当成人工授权；仓库只保存不含直接身份的 receipt ID/摘要。原视频、直接身份、凭据和本机媒体绝对路径不得进入 Git；
   - 建立独立的 authorized-development 配置/入口和 evaluator 控制流，但只用 synthetic metadata、synthetic tracking/annotation 与私有 fake runtime 测试。生产 CLI 必须拒绝 `synthetic_fixture`，不得暴露 `--allow-synthetic`、`--fake-runtime` 或公共 loader 绕过参数；原 M0-CAM-E/H synthetic-only 配置和 CLI 永不放宽；
   - 正式入口顺序固定为 `final 不存在/固定配置 → receipt approved+active+development-purpose 与允许范围 → collection/session manifest + media sidecar authorization-only 交叉绑定 → source/candidate preflight → tracking/annotation/grouped cohort → candidate loader → QC/forward → evaluator → staging/final`。receipt 缺失、失效或 ID/范围不匹配时，tracking、annotation、cohort、loader 和 forward 均为零调用，也不得创建 staging/final；
   - evaluator core 只实现参数化、纯函数式协议：确定性一对一 episode matching（含固定 tie-break）、shape 四分类/二分类原始计数与 confusion、purposeful hard-negative 独立诊断、ready/unavailable/inference_error/uncertain 的 coverage、区间运算和端到端阶段计时字段。production matching/uncertain/merge policy 未显式提供时拒绝正式评分；单测只传 test-only 参数。RD 不选择真实 IoU/onset tolerance、uncertain threshold、merge gap，不做校准、域适配、真实延迟、camera F1/FAR/CI 或 95% 结论；
   - `eligible_negative_person_hours` 只可由显式 tracklet↔匿名 participant 绑定、participant-present、truth-decidable、ordinary-negative、QC-unavailable 区间及可对齐的跨机位时钟计算；未标注剩余时段不能自动当 negative，prediction uncertain 不缩短可评分负例分母。缺绑定或无法跨机位对齐时必须返回 `not_computable`；
   - purposeful pacing 等困难负例在 shape 四分类仍是 pacing、shape binary 仍是 wandering-like；只在未来 alert-purpose 诊断中单列。未实现上下文告警层时不得输出 alert F1/FAR，也不得为减少误报把它改成 direct/non-wandering；
   - 本阶段允许普通代码、fixture、路径、timeout 和测试问题反复修复，不设一次性调用或全仓 pytest 门禁。不重训、不改固定 candidate、不修改共享 detector/tracker、跌倒模块或共享依赖，不 commit/push。

4. **M0-CAM-D：authorized camera development evaluation**
   - 工程 smoke 只有在 M0-CAM-RD 通过、C0 授权成立且 C1 已形成合法 tracking/sidecar 后才能运行；有标签评估还必须取得 C2 annotation 与 C3 grouped development manifest。文件存在或文件名含 `camera/dev/pilot` 不等于获得授权；
   - 工程 smoke 顺序为 `C0 approved receipt/reference → C1 tracking/sidecar → 独立 development CLI → 固定 primary → decode/tracking/QC/coverage/forward/latency`；有标签分支随后增加 `C2 episode annotation → C3 grouped development manifest → metrics/errors → development-only provisional policy`；
   - 使用 development 数据验证 tracking/QC coverage、ready/unavailable/inference_error、逐窗与 shape-episode 指标、purposeful hard negatives、eligible negative person-hours、端到端延迟和目标机位域差异；
   - 没有合法 annotation 时只能形成 `authorized_development_smoke`，不能输出 accuracy/F1、不能调阈值；有标签且评估闭环通过后才可写 `authorized_labeled_development_evaluated`；
   - uncertain、episode merge、匹配和任何阈值只能用 development 数据形成 provisional 版本。模型与规则冻结后才可进入 C4 sealed camera，sealed 结果永不回调同一版本。

整个 M0-CAM 的证据范围至少区分 `synthetic_contract_only`、`synthetic_schema_contract_only`、`authorized_development_smoke` 和 `authorized_labeled_development_evaluated`。M0-CAM-E/H 只写 `synthetic_contract_only`，M0-CAM-RD 只写 `synthetic_schema_contract_only`；后两项只能由 M0-CAM-D 在明确授权的真人 development 数据上形成。

| sidecar `authorization_status` | 既有 adapter `validation_scope` | 报告 `evidence_scope` |
| --- | --- | --- |
| `synthetic_fixture` | `synthetic_camera_contract` | E/H 为 `synthetic_contract_only`；RD 工具报告为 `synthetic_schema_contract_only` |
| `authorized_camera_engineering_smoke` | `authorized_camera_engineering_smoke` | `authorized_development_smoke` |
| `authorized_camera_labeled_evaluation` | `authorized_camera_labeled_evaluation` | `authorized_labeled_development_evaluated` |

三列属于不同字段空间，不得修改既有 adapter enum 来追求同名，也不得把 synthetic receipt/fixture 当作负责人的真人授权。
sidecar 的授权状态只表示允许的最高用途，不直接决定最终证据等级：即使 sidecar 允许 labeled evaluation，缺少合法 annotation、grouped manifest 或成功 evaluator 时也只能降为 smoke 或拒绝 evaluation mode，不能自动晋升为 `authorized_labeled_development_evaluated`。

### 7.5 仅在 M0 未达标时进入 M1

**M1+ 冻结压力集边界：** 如果性能实验使用 Step8 合成污染评估，只能复用既有 `data/processed/wandering/augmentation/v3/validation_pressure.jsonl`：278 个 validation parent 的 low/medium/high 各一次，共 834 条。ready 记录中的 `semantic_status=accepted|rejected`、QC 失败记录中的 `qc_status=unavailable` 与 `semantic_status=not_run_qc_unavailable` 必须全部保留。每个 severity 的资产完整性和总覆盖分母是 278；任务指标仍按 eligible cohort 分开计算，WP 四分类和 WP 二分类的分母分别均为 240，SmartCare 二分类分母为 38。二分类按来源分别报告，需要总指标时来源等权，不能按样本数池化；不得把 SmartCare 样本混入四分类。不得挑 seed、重采样、删除失败记录，或因模型结果不佳重新生成压力集。新 train-only 增强可以另建版本，但不得改变这套 validation 压力集。该文件是 ignored 验证资产；fresh checkout 缺失时应报告并取得既有冻结副本，不能现场生成替代集。该压力集只作合成污染诊断，不是目标摄像头证据。

按短板选择改进：

| M0 结果 | 优先改进 |
| --- | --- |
| loss 不收敛 | 学习率、归一化、loss 权重、初始化和数据映射 |
| direct 与 pacing 混淆 | 一阶差分、方向反转、往返次数、全局拓扑特征 |
| lapping 与 random 混淆 | 闭环稳定性、回访图、DWT/频域或曲率摘要 |
| 少数类别 recall 低 | class weight、balanced sampler、困难样本 |
| binary 强、subtype 弱 | 加 26 维手工拓扑特征的小 MLP late fusion |
| binary gate 拖累四分类 | 直接四分类主头 + binary 辅助头 |
| clean 好、tracking 污染差 | train-only 丢点、碎片、抖动和遮挡增强 |
| Transformer 成本高但无收益 | 先减层/减宽；MultiRocket 或轻量卷积只作诊断基线或 late-fusion 候选，不在未更新路线时替换 TCN + Transformer 主体 |
| Step10-A 初始化限制收敛 | 同结构 scratch 诊断 |
| 公开数据好、摄像头差 | 授权 camera development 数据微调或域适配 |

### 7.6 组件保留规则

组件可以保留，当且仅当：

- 目标指标提高；
- 对应错误类型确实减少；
- 其他关键类别没有明显崩溃；
- 训练和推理成本仍可接受。

如果复杂神经模型始终明显弱于 RF：

- RF 保留为强基线或 fallback；
- 不因模型更复杂而强行替换；
- 可将 RF 强拓扑特征作为神经模型融合输入；
- 仍以真实摄像头结果决定最终模型。

---

## 8. Windows / WSL 工程路线

### 8.1 核心原则

训练进程、源码、训练数据、checkpoint 和日志必须尽量位于同一个操作系统和文件系统中。

当前不再把“WSL 进程长期读取 `/mnt/c`”作为正式训练路线。它会造成：

- 跨文件系统小文件 I/O 变慢；
- checkpoint 和日志写入变慢；
- Windows/WSL 路径与权限混乱；
- CRLF/LF 反复变化；
- PowerShell→WSL→shell→Python 多层引号错误。

### 8.2 推荐执行平面

如果 `eldercare-ai` 主要在 WSL：

```text
~/work/eldercare-wandering/        WSL ext4 源码
~/data/eldercare-wandering/        WSL ext4 训练数据缓存
~/runs/eldercare-wandering/        WSL ext4 日志与 checkpoint
```

桌面仓库是源码事实源，WSL ext4 clone 是训练执行副本。做法：

1. 在 Windows 桌面仓完成实现、窄测并形成可达 commit；
2. WSL ext4 clone fetch 并 checkout 该 exact commit；
3. 将 M0 必需的 ignored preprocessing/split 数据一次性复制到 WSL ext4；只有实验已明确启用 A-init 且候选位置和兼容性均已验证时，才额外复制登记过的 Step10-A 候选；
4. 开跑前只检查 commit、editable 路径、数据路径、设备和剩余磁盘；
5. 长训练只读写 ext4；
6. 训练完成后将小型报告、metrics 和候选 checkpoint 一次性导回桌面仓的对应报告/制品位置；
7. 若训练中必须改代码，先在桌面仓修复并形成新 commit，再让 WSL clone 更新；不同时编辑两套源码。

这里不要求 push。未推送的桌面 commit 可以由 WSL clone 通过 `/mnt/c/.../心理算法` 这个只读 local remote fetch，或用一次性 `git bundle` 传入 ext4；传输完成后长训练不再从 `/mnt/c` 读取源码、数据或 checkpoint。

如果 Windows 原生 `eldercare-ai` 已通过核心依赖和窄测，也可以完全使用 Windows。禁止同一次实验把源码放 Windows、数据放 WSL、checkpoint 又写回 `/mnt/c`。

### 8.3 命令规则

- 正式逻辑写成仓库内 `.py` CLI 和 YAML；
- 不使用超长 `python -c`；
- 不使用 PowerShell→WSL→`bash -lc` 多层字符串；
- 直接在 WSL shell 中运行 WSL 命令；
- 日志实时输出并写文件；
- 训练应支持从 latest checkpoint 恢复；M0-S 已验证 CPU checkpoint/index 异常窗口恢复并明确 `cuda_resume_supported=false`，因此只主张 CPU 路线的现有能力，不声称 CUDA resume。

### 8.4 换行和复现

普通开发只要求：

- 源码、YAML、Shell 使用 LF；
- 用 `.gitattributes` 声明稳定规则；
- 提交前运行 `git diff --check`；
- 代码修改跑相关窄测。

不再把以下内容作为日常性能实验门禁：

- 每个文件的 raw-byte SHA；
- A/B 双环境逐字节输出一致；
- 每次只读 API 的全局调用次数；
- 每个阶段单独 Git 检查点；
- 27+30 个依赖工件逐项 URL 审计；
- 一次普通故障就停止并重新授权。

最终发布候选只需保留：

- Git commit；
- 可读配置；
- 环境版本或容器；
- seed；
- 数据/split 版本；
- best checkpoint；
- predictions 与 metrics；
- 必要 artifact checksum。

---

## 9. 轻量治理和 AI 自主权

仓库级执行规则以根目录 `AGENTS.md` 为准。本节只说明这条技术路线需要的最低治理强度；如措辞冲突，不在本文复制修补，以 `AGENTS.md` 和用户最新要求为准。

### 9.1 执行 AI 可以自主处理

在 `mental_health.wandering` 范围内，AI 可以：

- 实现 runner、config、loader、evaluator、test 和 report；
- 修复路径、引号、timeout、设备和局部代码问题；
- 调整 batch size、线程数、学习率、日志间隔、checkpoint 间隔和 early stopping；
- 从 checkpoint 恢复或重跑失败训练；
- 使用新的实验输出目录；
- M0 未达到两项 `0.95` 时按真实短板尝试一个主要改进；M0 已达标时直接完成 M0-S，不为了增加组件而启动 M1；
- 运行窄测、smoke 和 validation；
- 更新任务表和对应实验报告。

普通错误应自主诊断、修复、复测和继续，不必逐次请示。

上述调参和失败重跑自主权不适用于 M0-S 的合法完整 run。M0-S 以第 7.2 节固定项为准：工程失败、未形成完整 train + fresh reload 的无效 run 可以修复后在新目录重跑；已经完整结束的合法低分 run 必须保留并计入稳定性，不能改参数或重跑选分。

### 9.2 必须暂停的情况

只有以下情况需要负责人：

1. 修改标签语义、split、人工裁决或主要科学目标；
2. 读取 WP public holdout/raw 或 SmartCare official/raw 用于调参；
3. 删除、覆盖或改写原始数据、旧模型和既有证据；
4. 修改跌倒模块、共享心理评分或其他协作者目录；
5. 修改共享 `environment.yml`、共享依赖锁或共用环境包；
6. 采集真人视频但授权、存储和退出删除规则尚未批准；
7. push、发布、对外宣称摄像头/真实老人/临床效果。

### 9.3 验证强度

| 工作 | 最低验证 |
| --- | --- |
| 局部模型代码修改 | 相关窄测 |
| 训练链实现 | 窄测 + overfit smoke |
| 单次性能实验 | smoke + validation evaluator |
| 有前景候选 | 3 seeds + 相关徘徊测试 |
| 最终合并/候选级发布 | 完整测试、fresh reload、固定 WP public-holdout 结果和必要制品检查 |

完整 pytest 不需要在每个小实验后运行。

---

## 10. 目标摄像头数据路线

摄像头数据线与 M0 并行。其目的不是先建立复杂平台，而是尽快回答：

1. 目标机位能否稳定产生可用轨迹；
2. 哪些真实活动会被误报为徘徊；
3. 模型在连续背景下每小时产生多少误报。

### 10.1 C0：授权和存储

采集前由负责人明确：

- 采集目的；
- 参与者范围和知情同意；
- 退出与删除方式；
- 拍摄区域和敏感区域；
- 是否保留音频、人脸或原始视频；
- 原视频存储位置；
- 访问人员；
- 保存期限；
- 匿名 participant/session/setup 编码。

原视频不进入 Git。Git 只保存不含直接身份信息的配置、匿名 manifest、annotations 和报告。

### 10.2 C1：小型工程 pilot

#### 10.2.1 C1 必须复用的 tracking 输入契约

C1 不另建一套检测/跟踪 schema。每段视频或 stream epoch 必须形成一对可由现有 `load_camera_inputs()` 读取的 `TrackObservation JSONL + wandering-media-v1 sidecar`。完整字段和校验以 [camera_adapter.py](../../../../src/elderly_monitoring/modules/mental_health/wandering/camera_adapter.py)、[wandering_camera_v1.yaml](../../../../configs/modules/wandering_camera_v1.yaml)、[camera adapter 测试](../../../../tests/test_wandering_camera_adapter.py)和[步骤 7 实现报告](../../../../reports/mental_health/wandering_step7/README.md)为事实源。

- tracking JSONL 至少提供 `frame_id/track_id/bbox/track_confidence/timestamp_sec`。`bbox` 使用画面像素坐标 `[x1,y1,x2,y2]`；同一完整 scope 内 frame 和 timestamp 严格递增。
- sidecar 保存 `source_video_id/source_group_id/device_id/setup_id/stream_epoch`、去标识 `media_ref`、视频与 tracking 哈希、宽高/FPS/时长、`capture_started_at/timezone`、坐标系、detector/tracker、相机状态、授权和去标识状态。不得保存 token、直播 URL、请求头或本机绝对路径，也不得从 bbox 最大值猜画面尺寸。
- `timestamp_sec` 是当前视频或 stream epoch 内的相对时间；墙上时间只由 sidecar 的 `capture_started_at + timezone` 表达，未知时保留 null，不得猜测。
- 轨迹隔离键固定为 `(source_group_id, source_video_id, device_id, setup_id, stream_epoch, track_id)`。不同 video/setup/epoch 的同号 `track_id` 不合并；ID 改变生成新 tracklet，不自动推断为同一人。
- 上游 tracking 行里的 `person_id` 只是临时 track 别名，不能作为 participant identity、跨视频身份、日级聚合键或业务 person ID。稳定 participant/person identity 只能来自授权采集 manifest 或人工绑定，并与短时 `track_id` 分层保存。
- 采集 manifest 必须显式记录 `session_id → source_group_id`、`camera_setup_id → setup_id` 的映射，并用 `source_video_id` 连接 tracking 与 annotation。视频流重新打开或重连时使用新的 `stream_epoch`；机位、镜头或安装位置发生实质变化时使用新的 `setup_id`。
- 徘徊模块只调用共享 `run_yolov8_bytetrack()` 或消费兼容 JSONL；不得为本任务修改 `fall_risk/tracking.py`、pose 主路径或实时跌倒链。sidecar 和采集适配只在 `mental_health/wandering/` 与 `scripts/wandering/` 范围实现。
- C1 验收至少要求输入对通过现有 adapter/QC 窄测，并保留 fragment、suspected ID switch、camera moved/not_checked 等质量状态；不能通过丢弃坏行制造“完整”数据。

#### 10.2.2 Pilot 内容与验收

第一轮：

- 1 个固定机位；
- 1–2 名已授权成年人；
- 日间和较暗光线；
- direct、pacing、lapping、random-like；
- 找东西、电话踱步、清洁、锻炼、重复搬运等 purposeful hard negatives；
- 遮挡、进出画面、多人交叉、轨迹碎片和短 ID switch；
- 至少一段连续普通活动录像，用于误报分母。

每种脚本先做少量重复，重点检查：

```text
video
  → person detection / tracking
  → bbox-bottom trajectory
  → camera QC
  → 80-point sample
  → annotation alignment
```

C1 的最低验收在 tracking/sidecar 与 annotation alignment 接口可核验时完成。当前 primary 已冻结；RD+C0+C1 通过后可进入独立 M0-CAM-D 工程 smoke，取得 C2/C3 合法真值与分组后才进入有标签评估。RF/TCN 只保留 `comparison_only`，不再作为默认模型效果路径。pilot 只证明工程链可运行，不进入 sealed，不代表真实老人效果。

### 10.3 C2：标注格式

主标签以 episode/segment 为单位，不逐个标注重叠窗口。

最小字段：

```text
annotation_id
source_video_id
participant_id
session_id
camera_setup_id
start_sec
end_sec_exclusive
observable_pattern
purpose_context
purpose_evidence
evaluation_role
script_type
visibility_quality
tracking_issue
annotation_status
annotator_id
reviewed_by
```

标签轴：

```text
observable_pattern = direct | pacing | lapping | random | unknown
purpose_context = purposeful | nonpurposeful | unknown
purpose_evidence = scripted | participant_report | observed_context | unknown
evaluation_role =
    wandering_like_positive
    | purposeful_hard_negative
    | ordinary_negative
    | uncertain
    | excluded
```

`purpose_context` 不能根据轨迹形状猜测。只有采集脚本、参与者说明或明确可观察的活动上下文能够支持 purposeful/nonpurposeful 时才填写，并在 `purpose_evidence` 记录依据；证据不足统一为 `purpose_context=unknown`，不得进入告警决策层的正负指标。

轨迹形状和行为目的必须分开：

- 打电话时来回走可以是 `observable_pattern=pacing`；
- 同时为 `purpose_context=purposeful`；
- 评价角色为 `purposeful_hard_negative`；
- 不得为了方便分类把它改成 direct；
- 不创建 mixed 第五类；
- annotation 的 unknown 不等于运行时类别，运行时无法判断仍输出 uncertain/null。

当前 TCN + Transformer 只接收轨迹，不能从同样的 pacing 形状中推断“有目的”或“无目的”。因此 `purposeful_hard_negative` 用于暴露告警层所需的上下文和误报，不得在轨迹形态指标中当作 direct。若尚未实现上下文/episode 决策层，应如实报告这类误报，不能要求轨迹分类器完成它没有输入信息的任务。

### 10.4 C3：development 数据

- pilot 全部属于 development；
- participant、session/date、setup 在切窗前分组；
- 同一 episode 的窗口不能跨 split；
- development 可以用于错误分析、阈值、训练增强和摄像头域适配；
- 目的性困难负例必须单独统计；
- 连续负例时间不能只由精选短片组成。

建议先逐步形成：

- 每个 wandering subtype 至少 10 个可评分 episode；
- purposeful hard negatives 至少 30 个 episode；
- 至少 5 个 eligible negative person-hours；
- 正例、困难负例和 uncertain 由第二人复核。

这些是首批规划，不是临床样本充分性证明。

在首次 episode 评估前建立一份简短的 `episode_eval_v1` 配置，并只用 development 数据确定：

- 哪些既有 Camera QC 状态映射为 `unavailable`；
- 低置信度何时输出 `uncertain`；
- 相邻同类窗口合并为一个 episode 的最大间隔；
- prediction 与真值的一对一时间 IoU 或 onset/offset 容差；
- ordinary negative、unknown、多行为重叠和重复预测的计数方式。

数值不在本文提前猜定，但必须在打开 sealed 结果前冻结。这样既保留开发自主权，也保证最终分子和分母可解释。

### 10.5 C4：sealed 摄像头评估

在模型、阈值、episode 合并和匹配规则冻结后：

- 使用未参与训练或调参的 participant/session/setup；
- 轨迹形态层计算四类 episode precision、recall、F1 和 subtype macro-F1，purposeful pacing 仍按 pacing 形态计；
- 只有实现了场景/活动上下文或 episode 告警决策层，才另外按 `evaluation_role` 计算 wandering-alert episode precision、recall、F1；
- 对最终告警输出计算 false alarms / eligible negative person-hour；若告警层尚未实现，只能把“将 wandering-like 形态直接当告警”得到的数值标为诊断性 shape-trigger 误报率，不能冒充端到端系统指标；
- 报告延迟、coverage、uncertain 和 tracking 失败；
- 给出原始计数和置信区间。

必须区分真值不确定和模型拒识：`annotation_status/evaluation_role=uncertain|excluded` 表示人工真值不可用于主指标；`prediction_status=uncertain` 表示模型在一个可能具有可评分真值的样本上拒识。对真值可评分的 episode，预测为 uncertain 不能从 precision/recall/F1 分母删除：正例按未检出计入 recall，负例不记 false alarm，但必须保留在 eligible negative 时间中并计入 abstention/coverage；四分类 uncertain 同样按未正确分类/拒识报告。否则模型会通过大量拒识虚构 0.95。

C4 开始前根据希望得到的置信区间宽度，写明目标正例 episode 数、参与者/setup 数和 eligible negative person-hours。样本不足时仍可运行，但结论必须标为 descriptive pilot，不用 0.95 点估计冒充稳定达标。

`eligible_negative_person_hours` 只累计：

- 授权目标在画面中；
- 人工真值可判；
- 跟踪/算法可运行；
- 真值为 non-wandering；
- 排除正 episode、人工真值的 uncertain/excluded 区间以及 QC/tracking unavailable 区间；不得因为模型输出 `prediction_status=uncertain` 缩短分母；
- 多窗口或多摄像头重叠时间不重复。

若 eligible negative 区间零误报，95% Poisson 上界近似为 `3/T`。因此短 pilot 的零误报不能证明长期误报率很低。

### 10.6 摄像头开发建议文件

M0-CAM-E/H 的 primary 配置、wrapper、CLI、episode candidate 和测试已经实现；以下采集/标注/分组工具尚未实现，属于 M0-CAM-RD。其中 `prepare_camera_session.py` 只生成或核验现有 JSONL + sidecar 输入对，不重新实现 detector/tracker：

```text
configs/data/wandering_camera_collection_v1.yaml
src/elderly_monitoring/modules/mental_health/wandering/camera_dataset.py
src/elderly_monitoring/modules/mental_health/wandering/camera_development.py
scripts/wandering/prepare_camera_session.py
scripts/wandering/build_camera_annotations.py
scripts/wandering/run_camera_development.py
tests/test_wandering_camera_dataset.py
tests/test_wandering_camera_development.py
reports/mental_health/wandering_camera_data_readiness_v1/
```

RD 内部按 `RD-A 数据契约/readiness → RD-B authorized-entry 控制流 → RD-C evaluator primitives` 连续推进，不增加逐段人工审批。`prepare_camera_session.py` 在 RD 只 canonicalize/validate 调用者提供的 tracking+sidecar，不打开 MP4、不运行 detector；实际 C1 视频转换仍复用共享 tracker，并必须在真实 C0 receipt 通过后另行执行。

应优先复用现有：

```text
scripts/wandering/run_camera_inference.py
camera_adapter.py
camera_qc.py
camera_inference.py
```

不要复制一套新的 detector/tracker。

M0-CAM-E 已按以下独立文件实现 primary 接入，避免把冻结候选语义塞回历史 RF/TCN comparison-only 路径：

```text
configs/modules/wandering_camera_primary_v1.yaml
src/elderly_monitoring/modules/mental_health/wandering/camera_primary_inference.py
src/elderly_monitoring/modules/mental_health/wandering/camera_episode.py
scripts/wandering/run_topowander_camera_inference.py
tests/test_wandering_camera_primary_inference.py
tests/test_wandering_camera_episode.py
reports/mental_health/wandering_m0cam_engineering_v1/
```

文件名可按当前代码结构微调；稳定要求是复用现有 adapter/QC/preprocessing 与冻结 candidate loader，不修改共享 detector/tracker，不静默改变 Step7 v1 schema。

### 10.7 当前执行顺序

```text
M0-CAM-E/H（已完成；synthetic 模型契约）
  → M0-CAM-RD（当前；synthetic 数据 schema 与工具）
  → C0 授权 receipt
  → C1 tracking + sidecar
      ├─ M0-CAM-D 无标签 engineering smoke
      └─ C2 episode annotation
          → C3 grouped development manifest
          → M0-CAM-D 有标签 development 评估
          → 冻结模型、uncertain、episode merge 与 matching policy
          → C4 sealed camera
```

RD 与 C0-C3 可以并行推进：执行 AI 不因真人数据尚未交付而停止 RD；负责人也不需要等待 RD 才开始授权和采集安排。M0-CAM-D 工程 smoke 必须满足 RD+C0+C1；有标签评估必须再满足 C2+C3。两者都不得用 synthetic fixture、文件名或口头描述替代授权数据。

---

## 11. 最小交付物

### 11.1 每个保留的模型实验

```text
config.yaml
best checkpoint
last/resume checkpoint
validation_predictions
metrics.json
confusion_matrix
README.md
```

README 只需说明：

- 改了什么；
- 为什么改；
- 使用什么数据、split 和 seed；
- 指标是多少；
- 比当前最佳改善或退化多少；
- 是否保留；
- 下一短板是什么。

### 11.2 摄像头 pilot

```text
authorization summary
anonymous session manifest
scenario list
tracking/QC report
episode annotations
coverage and false-alarm denominator
pilot README
```

原视频只存受控位置，通过匿名 ID 引用。

---

## 12. 连续执行顺序

```text
模型线：
检查当前模型与数据
  → M0 overfit smoke
  → M0 单 seed 训练
  → best checkpoint
  → fresh reload
  → validation 报告
  → 判断是否达到 0.95
  ├─ 已达标：跳过 M1 → M0-S 固定 3 seeds → paired RF/TCN + 成本复核 → 冻结 development 候选
  └─ 未达标：按最大短板改一个组件 → 重新完成同口径 validation
  → M0-R Release-Prep（v2 已完成，不访问 test）
  → M0-RH 计分入口加固并生成 v3（已完成；未做候选 test 推理/计分）
  → M0-RS：固定 WP public-holdout 候选级计分（已完成，target_met）
  → M0-CAM-E primary camera synthetic 工程闭环（已完成）
  → M0-CAM-H 三项小型加固（已完成）
  → M0-CAM-RD camera development 数据就绪工具（当前）
  → M0-CAM-D 工程 smoke（等待 RD+C0+C1）/ 有标签评估（再等待 C2+C3）
  → C4 sealed camera

摄像头线：
C0 授权与存储
  → C1 工程 pilot
  → C2 episode 标注
  → C3 development 数据
  → M0-CAM-D development 评估
  → 模型/阈值冻结
  → C4 sealed evaluation
```

两条线并行：

- M0/M0-S/M0-R 模型线不等待摄像头数据；
- 摄像头授权和 pilot 不等待模型候选冻结；
- 未授权摄像头数据不得进入 M0、M0-S、M0-R 或 M1；
- development camera 数据进入训练前必须形成明确的新数据版本；
- sealed camera 数据永不参与训练和调参。

---

## 13. M0-RS 固定 WP public-holdout 计分协议（已完成）

本协议已于 2026-08-13 执行完成，状态为 `wandering_m0rs_public_holdout_scored`、`performance_status=target_met`。以下内容仅保留为该阶段的已关闭执行协议，不是当前任务；当前模型任务只看任务表。

> 不重训、不修改候选、不执行 M1，使用唯一 `score-frozen-wp` 入口对受信 accessor 返回的固定 240 条 WP public holdout 形成一份候选级计分结果。无论达到或未达到预注册门槛，都固化结果并结束本次计分；不得根据已经观察到的 test 分数或错误回调模型、阈值、split、标签、类别规则或协议。

执行上下文：

- M0-S primary 固定为 `seed=20260731 / best epoch=5`。M0-RH 已形成 v3 candidate manifest、release/training 双身份、7-entry source archive 和 validation parity；精确路径与 SHA 只以任务表和 M0-RH 报告为准，不在本路线文档重复维护；
- WP public holdout 是历史上已被契约测试解析、并被 RF/TCN 评分过的 public-shape benchmark，不是项目级首次盲测、目标摄像头、真实老人、产品或临床证据；
- 正式结果只含 WP cohort，因此只报告 WP 四分类与 WP 二分类指标，不计算 SmartCare source-equal test 指标。SmartCare official/raw、WP raw 和 sealed camera 继续禁止访问；
- 预注册通过条件为：WP 四分类 macro-F1 `>=0.95`、WP 二分类 macro-F1 `>=0.95`，且四个形状类别与 binary 两类的全部 recall 均 `>=0.90`；结果不满足时标记 `below_target`，但不在本 split 上调参重跑。

连续执行要求：

1. 开始前只读复核任务表登记的 v3 manifest SHA、M0-RH README/VERIFICATION/terminal audit、active source 与 7-entry archive、固定 model/config、Git HEAD/stage，以及正式输出目录不存在；保留全部用户脏改动和 v1/v2/v3 证据，不 stash/reset/覆盖；
2. 负责人在 Goal 中给出针对精确 v3 manifest SHA 的授权后，不再为 accessor 调用或阶段切换重复请示。只运行 M0-RH 报告冻结的 `score-frozen-wp` 命令，不改参数、不另建外部 records 入口；
3. controller 必须在 accessor 前完成输出不存在、manifest、candidate artifacts、active source、source archive、training/release 双身份、RF/upstream descriptors 和 CPU `8/1` + batch64 runtime preflight。preflight 失败且尚未打开 accessor/观察分数时，可在不改变候选与语义的前提下自主修复工程问题并继续；
4. accessor 返回记录后，记录 canonical records SHA、ordered sample-ID SHA、input/split/preprocessing identity、240 条计数和类别分布；随后完成唯一候选推理、WP-only evaluator、staging 校验和原子提交。final 输出已存在时只读核验，不再推理；
5. 分数提交后，按预注册口径生成 `target_met|below_target` 判定，报告四分类和二分类 macro-F1、全部 recall、confusion、逐样本 predictions/errors、原始计数及工程执行证据。不得打印、移动或修改正式机器制品来改变结论；
6. 正式结果固化后，才可读取 Step5 RF 与 Step6 TCN 中 primary seed `20260731` 的既有同 split predictions 做 post-hoc paired 解释；不得挑选其他 seed、重训 baseline 或把解释反馈成 WP test 调参；
7. 将叙述性报告与验证记录放在正式机器输出之外的独立报告目录，保持原子提交的正式输出文件集合不变。同步模块 README 和任务表；技术路线只在发现稳定设计缺口时做最小修正；
8. 阶段完成状态统一写为 `wandering_m0rs_public_holdout_scored`，另写 `performance_status=target_met|below_target`。无论哪种结果，下一模型工程方向都是 primary bundle 的 camera development 接入；真正独立泛化结论留给 C4 sealed camera。

普通路径、参数传输、timeout、进程监测、报告或 sidecar 问题由执行 AI 自主诊断和继续。只有需要改变固定候选/manifest、标签/split/指标/阈值、访问 WP raw/SmartCare official/raw/sealed camera、覆盖既有证据、修改其他模块、commit/push，或已观察分数后试图再次推理时才暂停。

---

## 14. 参考资料

- Bai, Kolter, Koltun. An Empirical Evaluation of Generic Convolutional and Recurrent Networks for Sequence Modeling. https://arxiv.org/abs/1803.01271
- Vaswani et al. Attention Is All You Need. https://papers.nips.cc/paper_files/paper/2017/hash/3f5ee243547dee91fbd053c1c4a845aa-Abstract.html
- Chen et al. Traj-MAE: Masked Autoencoders for Trajectory Prediction. https://openaccess.thecvf.com/content/ICCV2023/html/Chen_Traj-MAE_Masked_Autoencoders_for_Trajectory_Prediction_ICCV_2023_paper.html
- Weng et al. Whose Track Is It Anyway? Improving Robustness to Tracking Errors With Affinity-Based Trajectory Prediction. https://openaccess.thecvf.com/content/CVPR2022/html/Weng_Whose_Track_Is_It_Anyway_Improving_Robustness_to_Tracking_Errors_CVPR_2022_paper.html
