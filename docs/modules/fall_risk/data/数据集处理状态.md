# 跌倒风险数据集处理状态

更新时间：2026-07-25

本文按实际产物记录 `data/external/` 下的数据集处理状态。目录中存在原始文件不等于已经生成训练标签；“进入主标签链”要求至少完成统一 manifest、标签转换和 v3 训练视图接入。

## 状态汇总

| 数据集 | 当前状态 | 已完成 | 主要缺口 |
|---|---|---|---|
| LE2I/IMViA | 已进入主标签链，尚未冻结 | 190 个视频进入 manifest；v3 有 930 条动作和 214 条事件标签 | formal blocker 尚未清零，事件负样本不足 |
| Pre_VFallp | 已进入主标签链，来源受限 | 108 个视频完成 CVAT 导入；v3 有 246 条动作和 72 条跌倒事件 | 公开来源未验证；13 条 C03 不能自动作为 near-fall 正例 |
| CaucaFall | 已进入主标签链，尚未冻结 | 100 个视频、311 条人工动作和 311 条映射事件进入 manifest/v2/v3/split；10 个脱敏 CVAT ZIP 已保存 | 1 条 U01 缺少人工原因；事件 negative 和 near-fall positive 仍为 0，正式训练门禁未通过 |
| NTU RGB+D | 2,976 条人工精确动作已进入主标签链 | A008/A009/A042/A080 全片动作分别为 948/948/948/132 条，v3 父级和具体动作均为 primary；A043 948 条明确排除 | 外部 manifest 的旧解压路径当前不可用，需从 `ntu.zip` 重新解压或重建路径；C03 只作动作监督，不自动成为 near-fall 事件 |
| TOAGA | 部分接入 | 423 个资产进入 manifest；28 个步行视频生成 `normal_walk` auxiliary 标签 | 394 份姿态数据和参与者风险字段尚未形成正式训练数据集 |
| KINECAL | 独立实验已完成，效果未达标 | 55,509 个骨架帧完成校验；生成 50 人、219 窗口的数据集并训练 TCN | 未进入统一主标签链；测试 balanced accuracy 为 0.333，不能接入主路径 |
| Fall Detection 2017 | 仅完成下载和 manifest | 2,014 个视频进入 manifest，其中 2,012 个技术可用 | 未生成 v2/v3 标签或训练 split |
| UR Fall | 已进入主标签链 | 242 个资产进入 manifest；99 个视频完成 CVAT 导入，生成 268 条动作和 268 条映射事件并进入 v2/v3/split；`adl-07-cam0.mp4` 按人工决定略过 | 33 条 `U01` 不进入正式指标 |
| GSTRIDE | 仅完成 manifest | 475 个表格和时序资产进入 manifest | 跌倒史、TUG、SPPB、FES-I 和步态特征尚未转换为风险监督标签 |
| LTMM | 仅处理表格清单 | 2 个临床/居家报告表进入统一 manifest | 144 个加速度记录未下载；长期特征、风险标签和个体基线 split 为空 |
| Caicedo Gait | 尚未接入 | 原始 ZIP、Excel、C3D 和 CSV 已落盘 | 没有 manifest adapter、标签转换、processed 产物、测试或现行文档接入 |

## 状态分类

- 主标签链已接入：LE2I/IMViA、Pre_VFallp、CaucaFall、NTU RGB+D、UR Fall、TOAGA。其中 TOAGA 目前只提供 auxiliary 动作监督。
- 独立实验已完成：KINECAL。当前结果不满足进入实时步态主路径的条件。
- 仅完成 manifest 或清单：Fall Detection 2017、GSTRIDE、LTMM。
- 尚未工程接入：Caicedo Gait。

## 当前优先事项

1. 为 Fall Detection 2017 生成可追溯的 auxiliary clip 标签，并恢复 NTU RGB+D 的可用媒体路径；目录或文件名不能替代精确事件边界。
2. 人工补齐 fall/near-fall hard negative 和 near-fall positive，使事件训练门禁具备通过条件。
3. 将 GSTRIDE、LTMM 和 TOAGA 的参与者级字段转换为风险标签，解除 `functional_proxy_v1` 和 `longitudinal_baseline_v1` 的空数据阻塞。
4. 在确认来源、字段和参与者分组后，再决定是否接入 Caicedo Gait。

## 事实来源

- `data/manifests/fall_risk_video_manifest.jsonl`
- `data/annotations/fall_risk/action_labels_v3.jsonl`
- `data/annotations/fall_risk/event_labels_v3.jsonl`
- `reports/fall_risk/data_audit.md`
- `reports/fall_risk/training-labels-v3-validation.json`
- `reports/fall_risk/kinecal_gait_tcn/`
