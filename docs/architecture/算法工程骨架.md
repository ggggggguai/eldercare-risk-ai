# 算法工程架构

更新时间：2026-08-08

## 1. 工程边界

本仓库只实现跌倒风险和心理健康风险的算法原型。两个模块共享人员、设备、时间和事件字段约定，但各自评分并输出自己的 `AlgorithmEvent`。

算法工程负责数据适配、质量控制、特征提取、规则或模型推理、评估工具和标准化事件输出。业务系统负责账号、设备管理、数据库、消息通道、工单和人工处置。

## 2. 代码分层

| 目录 | 职责 |
|---|---|
| `common/` | YAML、JSONL 和 `AlgorithmEvent` 公共契约 |
| `modules/fall_risk/` | 跟踪、姿态、质量控制、步态、坐站、近跌倒、个体基线、跌倒内部评分，以及 manifest、标注校验、split 和事件评估 |
| `modules/mental_health/` | 行为/睡眠适配、日级聚合、个人基线和心理健康评分；`wandering/` 实验子另含版本化轨迹/来源/split、公开轨迹预处理、RF/TCN 对照，以及隔离的 bbox-only camera adapter/QC/离线推理链 |
| `runtime/` | 跌倒实时姿态跟踪、滑动窗口、采样/gap 诊断、episode 状态机、不可变事件版本和事件节流 |
| `service/` | 跌倒直播流读取、有界 latest-frame 队列、单会话 epoch 生命周期、进程内有界异步 outbox、FastAPI 和单次 HTTP 回调尝试 |
| `inference/` | 两个模块的离线命令行入口 |

依赖方向以公共契约和领域模块为内层，运行时和服务为外层。算法模块不访问业务数据库。

## 3. 跌倒风险链路

```text
视频帧
  -> YOLOv8-Pose + ByteTrack
  -> 主目标姿态与质量控制
  -> 10 秒内存窗口
  -> 步态 / 坐站 / 近跌倒 / 跌倒状态 / 个体基线 / 场景特征
  -> 规则 baseline 与模型化增强分支（TCN / MS-TCN++ / ST-GCN）
  -> 安全覆盖与风险融合
  -> fall_risk AlgorithmEvent
  -> episode 生命周期与不可变服务事件版本
  -> 有界异步 outbox 与后端 HTTP 回调
```

离线 JSONL 脚本仍用于数据处理和调试；实时主路径使用内存对象，不通过临时 JSONL 串联。

与推理链路并行的数据与评估控制链已经具备自动化实现：

```text
本地数据资产
  -> 确定性 manifest + 内容 hash + 媒体元数据
  -> 来源独立的候选标注导入
  -> schema / 来源 / hash / 技术排除严格校验
  -> fall_event / near_fall_event / functional_proxy / longitudinal_baseline 独立 split
  -> 版本化事件匹配、指标与失败案例 bundle
```

该控制链默认失败关闭。当前 v2 根标签 formal 校验仍为 `formal_ready=false`；四任务开发 split 中 `fall_event_v1`、`near_fall_event_v1` 为 provisional `ready`，`functional_proxy_v1`、`longitudinal_baseline_v1` 因缺少对应真值继续 `blocked`。独立的训练标签 v3 已通过 fall/near-fall 事件监督与无泄漏 split 门禁，但动作类型门禁仍未通过；上述产物均未冻结。合成数据评估只能证明工具链可运行，不能证明真实算法效果。

## 4. 心理健康链路

```text
带绝对时间的姿态记录 + 可选睡眠/自评
  -> 输入适配与质量校验
  -> person_id + 自然日聚合
  -> 3-14 日个人历史基线
  -> 活动、睡眠和规律性偏离评分
  -> 风险等级、置信度和人工复核建议
  -> mental_health AlgorithmEvent
```

当前入口是离线日级 CLI；仓库尚未实现心理健康定时调度或 HTTP 服务。

## 5. 当前实现状态

当前跌倒风险研发处于模型化增强阶段。模型增强优先覆盖步态、坐站、近跌倒/跌倒事件等时序分支；规则 baseline 继续作为对照、安全覆盖和低质量输入 fallback。个体行为基线和最终融合模型要等连续个人数据或 `risk_labels` 真值、无泄漏 split 及冻结评估协议具备后再替换。

| 能力 | 状态 | 说明 |
|---|---|---|
| 通用事件契约 | 已实现 | 模块编码只有 `fall_risk` 和 `mental_health` |
| 跌倒离线分层算法 | baseline 已实现；候选模型均为 shadow | 规则步态、坐站、近跌倒、跌倒状态、个体基线和模块内风险融合仍是运行主路径；候选模型未通过连续背景、老人域、冻结 test、延迟和稳定性等完整替换门禁 |
| RGB 步态 TCN | 六 seed 开发实验完成；未过门槛 | walking gate、条件异常头、质量隔离和 train-only 姿态增强已实现；B01-B04 functional proxy 集成 validation F1 为 `0.308`、正常误报约 `40.8/小时`，仅有 12 个正验证动作段且 test 未读取，默认 checkpoint 为 `null` |
| 坐站候选模型 | provisional clip 闭环完成；正式定位阻塞 | 已有 fail-closed 审计、`[16,14,7]` 数据集、E0、Logistic 和双头 TCN 训练/恢复/独立 validation。TCN pilot 只区分预裁剪 clip presence 与 A03/A04 方向，1,308/1,409 个 validation 事件来自 NTU；没有连续背景、真实定位边界或 test 评估 |
| 跌倒 candidate-clip TCN | v3 split 三 seed pilot 完成；不接入主路径 | `fall_action_presence_proxy_v1` validation F1 为 `0.958-0.963`，但只有 9 个 validation 保护组、来源以 NTU 为主，方向头未训练、test 未读取；该结果不是连续事件定位、误报率或真实跌倒确认指标 |
| 近跌倒恢复确认 TCN | v3 split 三 seed pilot 完成；不接入主路径 | validation F1 为 `0.990-0.997`，但 876 个开发窗口全部来自 NTU，实际负例窗口仅覆盖两类；模型在观察到恢复帧后确认候选，不是 onset-time 预警器，test 未读取 |
| KINECAL 步态 TCN 实验 | 已实现实验链，效果未达标 | 14 点数据准备、参与者级划分、训练和评估已跑通；7 人测试 balanced accuracy 仅 0.333，且任务是跌倒史 proxy；运行时任务校验禁止将其作为步态稳定性主模型 |
| 跌倒实时 HTTP 服务 | 阶段 0-3 核心实现已完成 | 单进程单路会话采用有界 latest-frame 队列和进程内有界异步 outbox；记录源 PTS/单调时钟、采样/gap、帧龄、目标四态、分支三态、融合 mask、episode 状态边、不可变事件版本、投递状态、epoch 和停止排空诊断。相同窗口已支持 memoization；新增帧后的真正增量计算、固定硬件收益、进程重启后的事件恢复和真实设备联调仍属后续工作 |
| 真实萤石直播联调 | 未完成 | 仍需业务侧直播地址、回调端点和真实设备验证 |
| 心理健康日级闭环 | 已实现 baseline | 支持行为、睡眠、自评、个人基线和缺失模态处理 |
| 心理健康徘徊专项 | 步骤 9+9a 检查点已形成；步骤 10 runtime 候选待 Git 闭合，训练未开始 | 步骤 5/6 的 RF 与纯 TCN 继续冻结为 `comparison_only`，步骤 7 仍只证明 `synthetic_camera_contract`；步骤 8 v3 的 A→V→H→C、1,491/834/240/48 和 12 条 `model_compatibility_warning` 保持不变。步骤 9/9a 已恢复两个 verified 状态，最终 `model.py` SHA-256=`11fd7732…80b331`。步骤 10 已冻结 1,257 clean + 1,491 v3 pair、126 proxy、input-mask、wrapper exact graph、config/artifact schema 与 canonical bytes；Linux/x86_64 runtime 候选=`configs/runtime/wandering_step10_runtime_v1.yml / 11426 bytes / 034d603a…e2f1cda` 已通过 27/30 工件来源审计，但尚未进入 Git 检查点和新 checkout 复验。关闭该门禁后才可开始步骤 10 红测，不建立 v4。当前证据仍不证明目标模型效果、目标摄像头、真实老人、episode、日级主链、心理风险或临床效果 |
| 心理健康在线调度 | 未实现 | 当前只提供函数和离线 CLI |
| 跌倒数据与评估自动化 | 已实现底座；服务于模型增强 | 已有统一 manifest、v2 候选导入/发布、训练标签 v3 迁移与校验、四任务 split builder、事件评估器和合成 bundle；108 条 Pre_VFallp 媒体已按内部授权例外全部技术可用并完成 CVAT 导入，共贡献 246 组动作/映射事件 |
| 跌倒真实数据门禁 | 事件监督通过；正式数据仍阻塞 | 根目录 v2 为 9,314 条动作、6,338 条事件；audit 结构错误为 0，formal `errors=0`、`blockers=285`。训练版 v3 为 9,314 条动作、9,498 条事件，split `valid=true` 且无跨分区泄漏；7 类 fall 和 8 类 near-fall hard negative 均有 primary 覆盖，`training_ready.fall_event=true`、`training_ready.near_fall_event=true`。部分稀有动作类型分区不全，`action_type=false`；连续背景、老人域、来源治理和冻结评估仍缺，不能声明 frozen、正式效果或长期风险预测就绪 |
| 跌倒正式指标 | 未就绪 | 尚无通过 formal 校验的真实标签、冻结 split、冻结协议和盲测治理证据；合成 bundle 不是比赛结果 |
| 临床有效性 | 未验证 | 所有输出均为风险提示，不是医学诊断 |

## 6. 现行约束

- 模型化增强阶段优先可验证的时序模型，但必须保留可解释、可复现和可降级的规则 fallback；复杂模型不能成为唯一安全路径。
- 缺失或低质量数据应降低置信度、限制等级或输出不可用状态，不能填成正常值。
- `person_id` 是跨时间聚合的业务身份；`track_id` 只表示单路视频内轨迹。
- 两个模块分别建立验证集、指标和阈值，不能用一个模块的结果证明另一个模块有效。

当前待办统一维护在[任务清单](../tasks/README.md)，对外字段见[算法事件输出接口](../interfaces/算法事件输出接口.md)。
