# 开发日志：2026-06-26

> 归档状态：早期开发日志，仅保留历史上下文。当前入口见[项目文档](../../README.md)。

## 今日目标

围绕“基于多模态 AI 监测的老年人跌倒风险、心理健康预警研究”，先把算法研发项目从方案文档推进到可运行、可验证的工程原型。今日重点放在跌倒风险模块：明确工程边界、建立输入输出契约、跑通从结构化特征到风险事件 JSON 的最小闭环，并为后续视频检测、跟踪和姿态关键点链路打基础。

## 完成工作

### 1. 搭建算法工程骨架

- 建立了项目顶层目录结构，覆盖 `configs/`、`data/`、`docs/`、`reports/`、`scripts/`、`src/`、`tests/` 等主要工程目录。
- 在 `README.md` 中明确本仓库只负责算法原型研发，不包含家属端 App、社区后台、账号权限、消息推送和工单流转。
- 补充 `docs/architecture/算法工程骨架.md`、`docs/interfaces/算法事件输出接口.md`，统一算法模块的事件化 JSON 输出边界。
- 为跌倒风险和心理健康两个模块分别建立模块说明、配置文件、样例特征和测试入口。

### 2. 明确跌倒风险算法主线

- 完成并更新 `docs/modules/fall_risk/plans/跌倒风险算法研发计划.md`，将目标从“跌倒后动作检测”扩展为“跌倒前风险预测、失衡中预警、跌倒后响应”的前置预警算法。
- 明确跌倒风险模块的主流程：

```text
视频或离线特征
  -> 人体检测与跟踪
  -> 姿态关键点
  -> 步态/坐站/转身/近跌倒特征
  -> 个体化行为基线
  -> 规则评分卡或轻量模型
  -> 风险事件 JSON
```

- 在 `docs/modules/fall_risk/README.md` 中固定第一版最小可用输入字段，包括 `gait_risk_score`、`sit_stand_risk_score`、`near_fall_event_score`、`baseline_deviation_score`、`scene_risk_score`、`activity_rhythm_score`、`fall_event_score`、`long_static_score` 等。
- 固定输出字段复用全局 `AlgorithmEvent` schema，包括 `risk_score`、`risk_level`、`risk_factors`、`confidence`、`trigger_event`、`recommended_action`、`model_version`。

### 3. 跑通跌倒风险规则评分卡原型

- 实现跌倒风险从结构化特征到风险事件 JSON 的最小推理闭环。
- 当前第一版采用可解释规则评分卡：
  - 加权融合步态、坐站、近跌倒、个体基线、场景和活动节律风险。
  - `near_fall_event_score >= 0.70` 时触发 3 级高风险提示。
  - `fall_event_score >= 0.80` 或 `long_static_score >= 0.80` 时触发 4 级紧急风险提示。
  - 置信度综合关键点质量、特征覆盖率和风险强度估计。
- 补充 `tests/test_fall_risk_pipeline.py`，覆盖近跌倒高风险、疑似跌倒/长时间静止紧急风险、特征覆盖率计算等关键行为。

### 4. 接入人体检测与跟踪采集入口

- 新增/完善 `src/elderly_monitoring/modules/fall_risk/tracking.py` 和 `scripts/collect/run_fall_tracking.py`。
- 使用 Ultralytics YOLOv8 + ByteTrack 作为当前工程主线的人体检测与跟踪后端。
- 输出 JSONL 轨迹记录，字段包含帧号、时间戳、`person_id`、`track_id`、人体框、中心点、像素速度、场景区域和跟踪置信度。
- 补充 `tests/test_fall_risk_tracking.py`，验证轨迹观测字段构造和 JSONL 写入行为。

### 5. 接入姿态关键点采集入口

- 新增/完善 `src/elderly_monitoring/modules/fall_risk/pose.py` 和 `scripts/collect/run_fall_pose.py`。
- 当前先使用 Ultralytics YOLOv8 pose 后端跑通姿态关键点结构化输出；研发计划中的 RTMPose 后续可作为适配器替换或并行接入。
- 支持输出 0-1 归一化关键点坐标，也可通过参数输出像素坐标。
- 输出 JSONL 姿态记录，字段包含帧号、时间戳、`person_id`、`track_id`、人体框、关键点列表、姿态置信度和关键点质量。
- 补充 `tests/test_fall_risk_pose.py`，覆盖关键点归一化、关键点质量估计、姿态观测字段和 JSONL 写入。

### 6. 建立样例数据与环境复现说明

- 新增 LE2I/IMViA 样例视频 manifest：`data/manifests/le2i_imvia_sample.jsonl`，记录样例视频路径、标注文件、场景区域、来源和许可。
- 下载/准备 `yolov8n.pt` 与 `yolov8n-pose.pt`，用于本地检测、跟踪和姿态烟测。
- 更新 `environment.yml` 和 `environment-reference.txt`，记录本机已验证的 `eldercare-ai` conda 环境、Python 版本和关键依赖版本。
- 在 `AGENTS.md` 中固定项目协作规则：测试和脚本运行必须使用 `conda run -n eldercare-ai python ...`，避免误用 base 环境或裸 Python。

## 验证情况

已补充的测试覆盖了当前最小闭环的核心行为：

- 跌倒风险规则评分卡输出等级、建议动作和解释因子。
- 跟踪观测字段构造与 JSONL 写入。
- 姿态关键点归一化、质量估计、观测字段和 JSONL 写入。
- 心理健康模块的工程占位与基础管线测试也已建立。

根据本地文件状态，今日已产生 pytest 缓存和脚本烟测相关产物，说明测试/验证命令已经被运行过。后续继续开发时仍需按项目规则显式执行：

```bash
conda run -n eldercare-ai python -m pytest -q
```

涉及检测或姿态链路时，还需要继续运行真实视频烟测：

```bash
conda run -n eldercare-ai python scripts/collect/run_fall_tracking.py \
  --input "data/external/le2i_imvia/sample/Videos/video (1).avi" \
  --output /tmp/fall_tracks_smoke.jsonl \
  --model yolov8n.pt \
  --scene-region home \
  --max-frames 5
```

```bash
conda run -n eldercare-ai python scripts/collect/run_fall_pose.py \
  --input "data/external/le2i_imvia/sample/Videos/video (1).avi" \
  --output /tmp/fall_poses_smoke.jsonl \
  --model yolov8n-pose.pt \
  --scene-region home \
  --max-frames 5
```

## 当前风险与限制

- 当前规则评分卡还没有使用真实老人跌倒风险标签训练，不能被描述为临床诊断模型。
- LE2I/IMViA 样例更适合验证跌倒检测、跟踪和姿态链路，不等同于长期跌倒风险数据集。
- 当前 `FallRiskPipeline.predict_from_features()` 是单次特征输入触发式推理，尚未实现实时视频流上的自动滑窗调度。
- 姿态后端暂用 YOLOv8 pose 跑通工程链路，后续仍需评估 RTMPose 或其他姿态模型在居家场景、遮挡、低光条件下的表现。
- 心理健康模块目前主要完成工程边界和基础占位，尚未形成与跌倒风险同等细度的研发计划和验证指标。

## 下一步计划

1. 将轨迹 JSONL 和姿态 JSONL 转成第一版可解释风险特征，优先实现步速、中心点位移、静止时长、人体框宽高比变化、关键点质量等基础特征。
2. 建立真实视频样例的端到端演示脚本：视频输入 -> 跟踪/姿态 -> 特征 -> 风险事件 JSON。
3. 为跌倒风险补充实验报告模板，记录样例视频、参数、输出事件、误报/漏报观察和后续修正。
4. 明确心理健康模块的特征体系、标注规范和非诊断式风险输出边界。
5. 后续如果接入真实老人数据，优先补齐脱敏、授权、数据划分和评估指标，不在日志或样例中写入可识别个人身份信息。
